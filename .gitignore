# --- environment secrets ---
server/.env
server/.env.docker
server/.env.local
client/.env
client/.env.local
client/docker-compose.yml

# --- node_modules (ไม่ต้องใส่ใน repo) ---
server/node_modules
client/node_modules

# --- Docker volumes / DB data ---
db_data/
*.sql

# --- misc build stuff (optional safety nets) ---
*.log
npm-debug.log*
dist/
build/
